{
  "name": "oryxbot-website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:platform": "pnpm --filter platform dev",
    "dev:pilot": "pnpm --filter pilot dev",
    "build:platform": "pnpm --filter platform build",
    "build:pilot": "pnpm --filter pilot build"
  },
  "devDependencies": {
    "@tailwindcss/oxide-linux-x64-gnu": "^4.2.1"
  }
}
